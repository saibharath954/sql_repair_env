# GRPO training dependencies (Colab / T4 / local)
# Install with:  pip install -r requirements-train.txt

# Core RLHF stack
trl>=0.12,<0.20
transformers>=4.45
datasets>=2.20
accelerate>=0.34
peft>=0.13

# Quantization (QLoRA on T4)
bitsandbytes>=0.43

# Plotting + utilities
matplotlib>=3.7
requests>=2.31
huggingface_hub>=0.25

# Notebook tooling
nbformat>=5.9
ipykernel>=6.29

# Torch is intentionally NOT pinned here — Colab/T4 already ships a CUDA-matched torch.
# If installing locally:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

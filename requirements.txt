# SQL Repair Environment — server dependencies
# Generated for Python 3.11. Pin versions for Docker reproducibility.

# OpenEnv core framework
openenv-core>=0.2.1

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP client (used by baseline_inference.py)
requests>=2.31.0
httpx>=0.27.0

# OpenAI client (baseline_inference.py)
openai>=1.30.0

# Pydantic (pinned to v2 — openenv-core requires it)
pydantic>=2.0.0

# Async support
anyio>=4.0.0

# Testing
pytest==9.0.3